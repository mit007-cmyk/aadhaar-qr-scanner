# Example app for the aadhaar_qr_scanner package.

Run from this directory:

```bash
flutter run
```

This demo app uses the `aadhaar_qr_scanner` package via a path dependency on the parent directory.
