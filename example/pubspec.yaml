name: aadhaar_qr_scanner_example
description: Demonstrates how to use the aadhaar_qr_scanner package.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.10.1
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  aadhaar_qr_scanner:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
